# 安全审计记录

审计范围：源码、资源、构建脚本、测试和待发布文件。

## 结论

未发现硬编码的个人 Cookie、账号 ID、房间 Cookie、Windows 用户目录、日志或遥测上报逻辑。源码中的默认 Cookie 仅为空模板，房间号默认值为 `-1`。

模组主动联网的目标为：

- B 站直播间初始化和 WBI 接口
- B 站用户信息接口
- B 站直播 WebSocket
- B 站头像 CDN

## 已修复的风险

弹幕消息中的头像 URL 来自远程数据。旧逻辑可能将登录 Cookie 复用到头像下载请求；如果 URL 被伪造，存在向非 B 站地址发送 Cookie 的风险。

当前版本已改为：

- 头像下载只接受 HTTPS 的 `*.hdslb.com` 或 `*.biliimg.com` 域名
- 头像图片请求不携带登录 Cookie
- B 站 API 请求仍按功能需要使用配置中的 Cookie

## 发布注意事项

真实 Cookie 只存在于用户本机的配置文件，不应进入版本库。发布包不包含 Minecraft 实例、日志、构建缓存、旧 JAR 或个人配置。

头像和弹幕内容会从 B 站传输到本地客户端并在 HUD 中显示，这是模组的预期功能，不属于额外的数据上报。
