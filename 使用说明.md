# BakaDanmaku 使用说明

## 运行环境

- Minecraft 26.1.2
- Fabric Loader 0.19.2 或更高版本
- Fabric API 0.155.2+26.1.2 或更高的 26.1.2 兼容版本
- Java 25
- 仅支持客户端，不需要安装在服务端

## 构建模组

在项目根目录执行：

```powershell
.\gradlew.bat clean build
```

正式模组位于 `build/libs/`，选择文件名不含 `sources` 的 JAR。将它和对应版本的 Fabric API 放入 Minecraft 实例的 `mods` 文件夹。

## 首次配置

1. 启动游戏并进入单人世界或服务器。
2. 退出世界，编辑 `config/bakadanmaku/bilibili.json`。
3. 修改 `room.id` 为 B 站直播间号，并将 `room.enable` 改为 `true`。
4. 重新进入世界，模组会连接直播间并显示弹幕。

配置修改后，在游戏内按 `B` 重载连接；按 `O` 打开设置界面。

## 登录 Cookie

游客鉴权可以接收弹幕，但 B 站可能隐藏部分昵称。需要完整昵称时，在自己的电脑上填写 `room.cookie` 中的 `SESSDATA`、`DedeUserID`、`DedeUserID__ckMd5` 和 `bili_jct`；`buvid3`、`buvid4` 留空时会自动获取。

Cookie 等同于登录凭据。不要把填写后的配置文件提交到 GitHub、截图或发送给他人。发布源码时只保留模组生成的空 Cookie 模板，不要上传本机 `config` 目录。

## HUD 设置

独立 HUD 模式支持：

- 拖动标题栏调整位置，拖动右下角调整窗口大小
- 弹幕文本自动换行
- 16 色陶瓦窗口与 16 色羊毛弹幕框
- 淡蓝色框架与米黄色便签纸预设“晴空便签”
- 独立 HUD 专用的头像显示开关
- HUD 背景和弹幕框分别设置透明度
- 原版字体或自定义加粗字体、文本大小和文本颜色

聊天栏模式下，HUD 外观和布局设置会自动禁用。

## 发布前检查

- 不要提交 `config/bakadanmaku/bilibili.json` 的真实副本。
- 不要提交 `build/`、`.gradle/`、`.gradle-user-home/`、`run/`、`logs/` 或本地 Minecraft 实例文件。
- 确认字体文件 `src/main/resources/assets/bakadanmaku/font/noto_sans_sc.ttf` 的 Apache License 2.0 许可随发布平台要求保留；字体文件元数据包含 Apache License 2.0 声明。
